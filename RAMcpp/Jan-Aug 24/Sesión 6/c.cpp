#include <bits/stdc++.h>
#include <iostream>
using namespace std;

#define ll long long int

void solve(){
    ll n , k; cin >> n >> k; 
    vector<int> a(n), b(n);

    for(int i = 0; i < n ; i++)
        cin >> a[i];

    for(int j = 0; j < n ; j++)
        cin >> b[j];

    
    
    
}

int main()
{
    int t; cin >> t;

    while(t--)
        solve();
    
    
    return 0;
}
